name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run CI script
        run: |
          chmod +x ./docker/ci.sh
          ./docker/ci.sh
